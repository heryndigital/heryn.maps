// Cole aqui a configuração do app web do Firebase
// (Firebase Console → Configurações do projeto → Seus apps → Web → firebaseConfig).
// Essa chave não é secreta: quem protege os dados são as regras do Firestore (firestore.rules).
export const firebaseConfig = {
  apiKey: 'COLE_AQUI',
  authDomain: 'COLE_AQUI',
  projectId: 'COLE_AQUI',
  storageBucket: 'COLE_AQUI',
  messagingSenderId: 'COLE_AQUI',
  appId: 'COLE_AQUI',
};
